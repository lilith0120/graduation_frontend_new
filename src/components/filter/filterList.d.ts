interface FilterList {
    file_name?: string,
    process?: Number,
    file_status?: Number,
}

interface ProcessList {
    id: Number,
    name: string,
}

interface TeacherList {
    id: Number,
    name: string,
}